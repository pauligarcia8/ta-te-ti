const Title = ({ children }) => {
  return (
    <h1 className="font-saira w-full text-5xl text-white text-shadow font-bold text-center m-4">
      {children}
    </h1>
  );
};

export default Title;
